# Test environment configuration - copy to .env.test
NODE_ENV=test
PORT=8001

# Test database (use a separate test database)
DATABASE_URL="postgresql://test_user:test_password@localhost:5432/adapt_v3_test"

# Mock AWS credentials for testing
AWS_ACCESS_KEY_ID=test_access_key
AWS_SECRET_ACCESS_KEY=test_secret_key
AWS_REGION=us-west-1
AWS_BUCKET_NAME=test-bucket

# Mock API keys
OPENAI_API_KEY=test_openai_key
CLERK_SECRET_KEY=test_clerk_key

# Disable external services in tests
QSTASH_ENABLED=false
GOOGLE_CLIENT_EMAIL=test@example.com
GOOGLE_PRIVATE_KEY=test_private_key
GOOGLE_PROJECT_ID=test_project

# Frontend URL for CORS
FRONTEND_URL=http://localhost:3000
